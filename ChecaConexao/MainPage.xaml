<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:local="clr-namespace:ChecaConexao" x:Class="ChecaConexao.MainPage">
    <StackLayout Padding="0,30,0,0"  Orientation="Vertical">
        <!-- Place new controls here -->


        <Image Source="{Binding Icon}"
           VerticalOptions="Center"
           HorizontalOptions="Center"/>

        <Label
            Text="{Binding Msg}"
            BackgroundColor="{Binding Cor}"
            VerticalOptions="Center"
            HorizontalOptions="Center" />

        <Label
            IsVisible="{Binding OnLine}"
            Text="{Binding Tipo}"
            VerticalOptions="Center"
            HorizontalOptions="Center" />
        
    </StackLayout>
</ContentPage>